# ss-generator
A static site generator that takes markdown files and turn them into proper HTML files. It translates inline and block level elements, supports all markdown types to proper HTML types.
